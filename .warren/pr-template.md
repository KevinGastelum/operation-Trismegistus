<!--
Warren PR-body template override (.warren/pr-template.md).
Parser: warren src/runs/pr-template.ts. Each `## <fragment>` H2 REPLACES that
fragment's default body with LITERAL markdown — this Warren build (v0.7.8) does
NOT interpolate {{tokens}}. Known fragments: title, summary, run, seeds,
preview_url_or_placeholder, commits, files_changed, prompt, trailer.
Omitted fragments keep Warren's dynamic defaults (summary/run/seeds/commits/
files_changed/prompt render real per-run data automatically), so we override
ONLY the trailer to add the pilot's human-review gate + checklist.
Do NOT re-add `## <name>` sections with {{...}} bodies — they emit literally
into the PR and fail the `warren_config` doctor check.
-->

## trailer

---

### Operator review — required before merge

- [ ] Reviewed generated changes
- [ ] Ran or verified tests
- [ ] No secrets / `.env` values added
- [ ] Branch scope matches the prompt
- [ ] Checked preview (if any)

🤖 Generated by Warren. **Human review required before merge** (pilot-gated repo).
