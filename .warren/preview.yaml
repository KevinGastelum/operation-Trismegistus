# Warren preview config for operation-Trismegistus.
#
# Leave commented unless this repo has a dev server Warren should expose after a
# successful run. Uncomment and adjust ONE example, then set the right port.
#
# Node / Vite / Next.js:
# type: server
# command: pnpm dev
# port: 3000
# readiness_path: /
# readiness_timeout: 2m
# connect_timeout: 30s
# idle_ttl: 30m
# max_lifetime: 8h
#
# Python / FastAPI:
# type: server
# command: uvicorn app.main:app --host 0.0.0.0 --port 8000
# port: 8000
# readiness_path: /health
