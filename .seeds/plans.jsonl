{"id":"pl-a703","seed":"operation-Trismegistus-555f","template":"feature","status":"approved","revision":1,"sections":{"context":"The Warren pilot model executes project phases as plan-runs (Q-A). Phase 0 and 1 are done, but the local pilot-layer artifacts the design calls for (a STATUS.md living-state file, a session guide, the operator vocabulary, and a status recipe) do not exist yet. This plan produces those genuinely useful artifacts AND serves as the first end-to-end validation of Warren's plan-run coordinator and the pilot's phase-close Codex gate, dogfooding Warren on its own control repo.","approach":"Run a short serial plan-run of three child seeds. Seeds 1 and 2 are low-risk docs/recipe tasks (real PRs, real serial blocking, real merge waits) that build the pilot-layer artifacts. Seed 3 is a phase-close gate marker whose PR the pilot HOLDS: at plan_run.waiting_for_merge the pilot stops, runs a Codex review of the cumulative phase diff, and merges only if clean. Quality gate is bash -n scripts/*.sh (zero-dependency, real). The pilot is the SOLE merger (no auto-merge), removing the gate race.","alternatives":[{"name":"Write the docs locally in the pilot session with no Warren","rejected_because":"Would not exercise the plan-run coordinator, which is the point of choosing the plan-run model in Q-A."},{"name":"Use a no-op true quality gate","rejected_because":"Codex: a no-op gate proves the pipeline can ignore quality rather than enforce it."},{"name":"Add branch protection and a required status check for the gate now","rejected_because":"Deferred to a later hardening pass; no-auto-merge plus pilot-sole-merger is safe for the v1 validator."}],"steps":[{"title":"Create STATUS.md (living pilot state) and docs/warren-integration/PILOT-SESSION-GUIDE.md","type":"task","priority":2,"blocks":[2],"labels":["pilot-layer"]},{"title":"Create docs/warren-integration/OPERATOR-VOCAB.md (>=10 operator intent phrases) and a just status recipe","type":"task","priority":2,"blocks":[3],"labels":["pilot-layer"]},{"title":"Phase-close gate: commit a phase-complete marker; pilot Codex-reviews the phase diff before merge (do not auto-merge)","type":"task","priority":2,"blocks":[],"labels":["pilot-layer","phase-close-gate"]}],"risks":["Warren host-side push needs the GITHUB_TOKEN PAT to include this repo (blocker being fixed); the plan cannot run until then.","Workers run the quality gate bash -n scripts/*.sh; keep scripts syntactically clean.","The gate relies on the pilot not merging seed 3's PR until Codex passes; mitigated by pilot-sole-merger."],"acceptance":["A plan-run with 3 child seeds dispatches and runs them serially (coordinator waits for each PR to merge before the next).","STATUS.md, PILOT-SESSION-GUIDE.md, OPERATOR-VOCAB.md (>=10 intent phrases), and a working just status recipe exist on main after the plan-run.","The pilot HALTS at the phase-close gate seed, a Codex review of the phase diff is recorded, and the gate PR merges only after it passes.","No PR is auto-merged; every merge is performed by the pilot."]},"children":["operation-Trismegistus-4b5c","operation-Trismegistus-99f4","operation-Trismegistus-45e5"],"createdAt":"2026-06-09T09:33:07.294Z","updatedAt":"2026-06-09T09:33:07.294Z","name":"Pilot-layer artifacts (plan-run validator)"}
